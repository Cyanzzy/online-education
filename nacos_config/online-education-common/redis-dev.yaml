spring:
  redis:
    host: localhost
    port: 6379
    password: root
    database: 0
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 0
    timeout: 10000
      #redisson:
      #配置文件目录
    #config: classpath:singleServerConfig.yaml
